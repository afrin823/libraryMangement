import LoaderCard from "./LoaderCard";

const Loader = () => {
    return (
        <div className="width-control relative">
            <LoaderCard />
        </div>
    );
};

export default Loader;
